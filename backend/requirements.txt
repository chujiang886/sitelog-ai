# BOIP backend 运行时依赖声明
#
# 本项目 backend 运行时依赖由 backend/.venv 预装提供（历史无统一依赖清单）。
# 本文件登记 Phase 2 新增的显式依赖：
#   - aiosqlite：SQLite 异步驱动（2.1.4 AsyncSession 引入，create_async_engine
#     默认回落 "sqlite+aiosqlite:///:memory:" 所必需）。
#   - greenlet：SQLAlchemy 异步引擎（AsyncSession/async_engine）运行所必需，
#     协程与同步 Session 切换的核心依赖；venv 预装缺失时需显式安装。
#
# 安装：
#   backend/.venv/bin/python -m pip install -r backend/requirements.txt
aiosqlite>=0.19
greenlet>=3.0
