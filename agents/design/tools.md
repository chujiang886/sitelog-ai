# Design Agent 工具契约

## Phase 0 工具状态

当前不启用设计引擎、知识检索、产品目录或工程计算服务。

## 未来接入规则

- 只调用已声明且通过评审的知识与设计工具。
- 工具输入必须保留上游证据关联，输出必须可追踪到规则版本。
- 未经确认的业务参数一律标识 `pending_verification`，不得驱动方案判断。
- 外部产品数据必须有合法来源与更新时间。
- 工具失败时停止相关建议，不用模型补全。
