# Node.js
node_modules/
.next/
.next.trash/
dist/
build/
coverage/

# Build / deliverable output (not version-controlled)
deliverables/

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# Runtime data (uploads, local object storage) — never version-controlled
backend/storage/

# Environment and secrets
.env
.env.*
!.env.example

# Logs and system files
*.log
.DS_Store

# IDE
.idea/
.vscode/

# ─────────────────────────────────────────────────────────────────────────────
# Phase 3.8.27 T4 仓库治理：以下条目均为「运行时可再生产物」，一律不入库。
# 判定标准（写死，后续新增条目须同样自证）：
#   1. 由程序运行/测试执行自动生成；
#   2. 删除后可通过重跑复现，不含唯一人工输入；
#   3. 内容随机（哈希名/时间戳）或体量持续膨胀。
# 不满足以上三条的一律**必须追踪**，尤其是源码、测试、治理文档。
# ─────────────────────────────────────────────────────────────────────────────

# 测试临时产物（历史债：曾堆积数百个 _tmp_* 文件导致 threshold 系列测试雪崩式失败）
# Phase 3.8.31 T7 已做根因修复：threshold 系列测试的临时件全部迁出仓库（pytest
# tmp_path / 系统临时目录），正常路径下不再向 tests/ 落盘。以下规则保留为防御层，
# 覆盖历史残留与个别用例回退时的产物泄漏。
tests/_tmp_*
tests/intake_snapshots/
# 迁移快照产物：`migrate_thresholds` 以 `verified.<sha256[:16]>.v1.json` 确定性命名，
# 由内容哈希生成、可重跑复现，不含唯一人工输入。注意与阈值 SSOT `verified.json`
# 区分——后者必须追踪，不受此规则影响。
tests/verified.*.v1.json

# 构建缓存（tsbuildinfo 每次 tsc 都变更，入库只会制造无意义 diff）
*.tsbuildinfo

# agents 运行时状态与审计流水（属运行数据，应落审计系统/数据库，不进版本库）
agents/engineering/knowledge/*_sessions.json
agents/engineering/knowledge/knowledge_items_pending.json
agents/engineering/thresholds/verified.json
agents/engineering/**/*.jsonl

# 发布流程运行时记录（由 scripts/release 生成）
release_freeze_record.json
release_candidate_record.json

# 本地 AI 工作台数据（会话记忆等，属个人环境，不属项目资产）
.workbuddy/
