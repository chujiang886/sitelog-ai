# 2.1.7 测试基线刷新报告（Phase 2.1 权威测试基线）

- **阶段**：Phase 2.1 · 任务 2.1.7 测试基线刷新
- **身份**：BOIP AI 质量工程负责人
- **执行时间**：2026-07-27 15:11 (GMT+8)
- **执行命令**：`bash scripts/ci/local_ci.sh`
- **执行环境**：macOS (darwin) · Python 3.11.15 (`backend/.venv`) · pytest 8.3.4 · Node/Jest（BOIP 根 workspaces）
- **前置状态**：2.1.4 AsyncSession 已验收完成（COMPLETED）；2.1.1 / 2.1.6 已 COMPLETED
- **结论**：✅ **第 1 轮即 8/8 全绿（`Local CI passed.`），无失败项，未触发分类修复流程，未降低任何测试标准。**

---

## 一、完整 8 步结果（权威基线）

> 注：脚本实际 8 步与任务书列举的名义 8 步一一对应关系如下表「对应任务书步骤」列。脚本第 2 步 pytest 同时覆盖 backend tests + agents tests + e2e（`testpaths: tests, ../tests/agents, ../tests/e2e`），即任务书的 "2. pytest" 与 "3. backend tests" 合并在脚本 [2/8] 中执行；脚本另含前端 ESLint（[3/8]）。

| # | 脚本步骤 | 对应任务书步骤 | 结果 | 关键数据 |
|---|---------|--------------|------|---------|
| 1/8 | Backend lint (Ruff)：`app tests ../agents ../tests/agents ../tests/e2e` | 1. Ruff | ✅ PASS | `All checks passed!`，0 违规 |
| 2/8 | Backend pytest + coverage（门槛 ≥60%） | 2. pytest + 3. backend tests | ✅ PASS | **151 passed / 0 failed**（24.12s）；覆盖率 **82.67%**（TOTAL 2441 stmts / 423 miss / 83%），门槛 60% 达标 |
| 3/8 | Frontend lint (ESLint / next lint) | — | ✅ PASS | 0 error / **1 warning**（非阻断，见 §三） |
| 4/8 | Frontend Jest + coverage（门槛 ≥50%） | 4. frontend tests | ✅ PASS | **6 suites / 29 tests 全过**（1.8s）；Stmts **93.15%** / Branch 78.57% / Funcs 100% / Lines 93.15% |
| 5/8 | Alembic upgrade head → downgrade base | 5. alembic | ✅ PASS | 3 个迁移（d17f02429ce9 → eb1c2d3e4f5a → c2f4a6b8d901）升/降双向全通 |
| 6/8 | Seed 脚本 | 6. seed | ✅ PASS | tenants 1 / users 1 / agents 4 / knowledge_rules 5 / knowledge_cases 5 / threshold_configs 1 |
| 7/8 | 编造业务数值 + key 指纹扫描 | 7. fabrication scan | ✅ PASS | 未发现未验证数值或凭证泄露 |
| 8/8 | 硬编码业务配置扫描 | 8. hardcode scan | ✅ PASS | 未发现业务阈值、品牌或型号 |

**终态输出**：`Local CI passed.`（exit 0）

## 二、151 个后端 pytest 用例构成（基线明细）

| 集合 | 用例数 | 说明 |
|------|-------|------|
| `backend/tests`（FastAPI） | **64** | 含 2.1.4 改造后的 conversations(11) / uploads(10) / vision_routes(7) 等 14 个文件 |
| `tests/agents`（Agent 运行时） | **86** | base/core/design/environment/loader/orchestrator/registry/report/router/vision 等 13 个文件 |
| `tests/e2e` | **1** | test_smoke_e2e |
| **合计** | **151** | 与 2.1.4 验收数（backend 64 + agents 86 = 150）一致，+1 为 e2e smoke（2.1.4 单独跑套件时未含） |

## 三、失败分类（A 代码 / B 测试 / C 文档误报 / D 配置）

**本轮无失败项，A/B/C/D 四类均为空。** 唯一非阻断观察项：

| 项 | 类别 | 内容 | 处置 |
|----|------|------|------|
| ESLint Warning ×1 | 观察项（非失败，不属 A–D） | `src/app/upload/page.tsx:181` 使用 `<img>`，`@next/next/no-img-element` 建议改 `next/image` | 不阻断 CI（warning 级）；属前端优化建议，**不在 2.1.7 范围内擅动前端**，登记为观察项，可在 Phase 2.2 前端任务顺带处理 |

## 四、测试标准确认（未降低）

- Ruff 检查范围未缩减（app + backend tests + agents + agents tests + e2e）。
- 后端覆盖率门槛保持 **60%**（实际 82.67%，余量 +22.67pt）。
- 前端覆盖率门槛保持 **50%**（实际 93.15%，余量 +43.15pt）。
- 未跳过/标记 skip 任何用例；未修改任何扫描白名单；未修改 `local_ci.sh`。

## 五、Phase 2.1 权威测试基线（后续阶段对照基准）

```
local_ci.sh        : 8/8 PASS（2026-07-27）
backend pytest     : 151 passed（backend 64 + agents 86 + e2e 1），coverage 82.67%（门槛 60%）
frontend jest      : 29 passed / 6 suites，coverage 93.15%（门槛 50%）
ruff / eslint      : 0 error（eslint 1 warning 观察项）
alembic            : head↔base 双向通过（3 迁移）
seed / 双扫描      : 全通过
```

> 后续 2.1.5 及 Phase 2.2 的任何提交，测试结果不得低于以上基线；新增功能须同步新增测试。

## 六、挂账债项处置（R6 / R7）

| 债项 | 原状态 | 处置 | 结果 |
|------|-------|------|------|
| R6 覆盖率门禁数据陈旧（仍引用 Phase0 基线） | medium | 本次重跑 local_ci.sh 刷新权威基线（backend 82.67% / 前端 93.15%） | ✅ RESOLVED (2.1.7) |
| R7 测试收集冲突（factories import，pytest 无法独立跑） | low | 实证复测：仓库根合并收集 `pytest backend/tests tests/agents tests/e2e` → **151 passed，无收集冲突**，可独立跑 | ✅ RESOLVED (2.1.7)，不复现（仅 1 条 `pytest.mark.e2e` 未注册 mark 的观察级 warning，权威路径 local_ci 下无此告警，未改动任何代码） |

## 七、状态更新与下一步

- `.ai/project_status.json`：2.1.7 → **COMPLETED**；`last_known_green_tests` 刷新为本基线。
- `.ai/roadmap_v2.md`：2.1.7 行标注 ✅ COMPLETED（2026-07-27）。
- **流程门禁**：按指令**不进入 2.1.5**，等待用户验收 2.1.7 后按序推进（2.1.5 → Phase 2.2，不跳阶段）。
