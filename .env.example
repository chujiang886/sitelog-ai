# All values are placeholders — pending_verification by deployment team.

# --- Database ---
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
DATABASE_URL=

# --- Cache / Vector / Object storage ---
REDIS_URL=
QDRANT_URL=
MN_ROOT_USER=
MN_ROOT_PASSWORD=
MN_BUCKET=
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=

# --- Auth ---
JWT_SECRET=

# --- LLM / AI gateway (Phase 1 / T06c) ---
# 留空表示未启用；填入真实 key 后需同步将 agents/config.yaml::llm.enabled 改为 true
# 并经过主理人（轩哥）验证后才能对外提供服务。
LLM_A_BASE_URL=
LLM_A_API_KEY=
LLM_A_MODEL=
LLM_B_BASE_URL=
LLM_B_API_KEY=
LLM_B_MODEL=

# --- App runtime ---
APP_ENV=
LOG_LEVEL=INFO